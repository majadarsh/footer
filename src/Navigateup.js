import React from "react";
import { TinyButton as ScrollUpButton } from "react-scroll-up-button";

class Navigateup extends React.Component {
  render() {
    return (
      <div title="Scroll Top">
        <ScrollUpButton />
      </div>
    );
  }
}
export default Navigateup;
